
public class Day1 {  // jadi setiap kode java itu harus ada di dalam class nah nama class nya itu Day1 harus sama dengan nama file
    //ini adalah entry point,java akan mengeksekusi kode kita di mulai dari sini

    /**
     * @author AkhtarFauzan
     * 
     */
    public static void main(String[] args) {
        int p = 10;
        int l = 5;
        int hasil;

        hasil = p * l;
        System.out.println(hasil);

        //single line comment  baris ini tidak akan di eksekusi

        /*
       multi line comment
       ini tidak akan di eksekusi
       ini tidak akan di eksekusi
       ini tidak akan di eksekusi
       ini tidak akan di eksekusi
       ini tidak akan di eksekusi
         */
    }
}
